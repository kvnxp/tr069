# TR-069 Management System Configuration
# Copy this file to .env and customize the values

# Server Configuration
PORT=7547
HOST=0.0.0.0

# CMS Configuration
CMS_SESSION_SECRET=change-this-secret-key-in-production

# Debug Configuration (optional)
DEBUG_MODE=false

# Production Security Notes:
# 1. Change CMS_SESSION_SECRET to a strong, random value
# 2. Update default CMS credentials in src/auth/session.ts
# 3. Enable HTTPS and set secure cookies for production
# 4. Configure firewall to restrict CMS access
# 5. Use environment-specific configuration files